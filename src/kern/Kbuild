obj-m := saltfs.o
saltfs-objs := dir.o inode.o string.o super.o user.o

ccflags-y += -DDEBUG

#CFLAGS := -DDEBUG  ## don't work
#CFLAGS_saltfs-objs := -DDEBUG ## don't work
#CFLAGS_super.o := -DDEBUG
#CFLAGS_inode.o := -DDEBUG